// To write a regular expression in JavaScript, you can put the expression between two slashes:

const regexObject = /^word$/
// or:
const regexObject = new RegExp("^word$")

// ---

// Literal:
/regexpString/

// Constructor:
new RegExp("regexpString")
