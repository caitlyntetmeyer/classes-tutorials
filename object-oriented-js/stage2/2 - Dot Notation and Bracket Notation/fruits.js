/*

You can get at the style, containing HTML, parent, and children of an element using the DOM:

element.style
element.innerHTML
element.parentNode
element.childNodes

DOM objects have methods, too:

getElementById()
appendChild()

Arrays are objects:

array.length
push()
pop()
concat()

*/

const fruits = ['apple', 'pear', 'strawberry'];
fruitsLength = fruits.length;

fruits[0]; // apple
